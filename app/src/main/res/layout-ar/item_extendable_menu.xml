<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    android:layoutDirection="rtl"
    android:id="@+id/ext_item"

    >

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/ext_item_img"
        android:layout_width="@dimen/feat_view_icon_width_height"
        android:layout_height="@dimen/feat_view_icon_width_height"
        android:contentDescription="@string/nav_header_desc"
        android:scaleType="fitXY"
        android:src="@drawable/logo"
        android:layout_gravity="center"
        android:layout_margin="10dp"

        />

    <TextView
        android:id="@+id/txt_title_menu_item"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="@dimen/search_tv_text_size"
        android:text="title"
        android:textColor="#000"
        android:layout_gravity="center"
        android:gravity="center"
        android:layout_margin="10dp"
        android:fontFamily="@font/cairosemibold"

        />
</LinearLayout>